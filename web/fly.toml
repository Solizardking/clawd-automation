# ⚠️ SUPERSEDED — deploy from the REPO ROOT instead.
#
#   cd /Users/8bit/automation
#   fly deploy
#
# The root /fly.toml uses dockerfile = "web/Dockerfile" with the repo root as
# the build context, so the image contains ooda/, agent/, constitution/, etc.
# This file exists only to explain why the original web/-scoped config was
# replaced (the web/-only context cannot see ooda/package.json).
#
# See /fly.toml for the current live configuration (app: clawd-automation,
# volume: automation_journal, region iad).